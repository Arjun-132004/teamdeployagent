apiVersion: v1
kind: Service
metadata:
  name: teamdeployagent
  namespace: storefront-devteam
  labels:
    app.kubernetes.io/name: teamdeployagent
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: teamdeployagent
  ports:
    - name: http
      port: 80
      targetPort: 3000
